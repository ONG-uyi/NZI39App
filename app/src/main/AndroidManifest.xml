<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nzy39.nzi39app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/k"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/c"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:testOnly="false"
        android:usesCleartextTraffic="true">
        <activity android:name=".Net.SignActivity">
        </activity>
        <activity android:name=".Net.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
            <activity android:name=".Net.list.NetListActivity">
            </activity>
        <activity android:name=".Net.WebActivity" />
        <activity android:name=".Net.NetActivity" />
        <activity android:name=".SeniorView.SeniorViewActivity"></activity>
    </application>

</manifest>